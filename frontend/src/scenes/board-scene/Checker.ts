export class Checker extends Phaser.GameObjects.Container {

    constructor(scene: Phaser.Scene, color: number, player: string) {

        super(scene);
    }

    public move(i:  number): void {
        //
    }
}
